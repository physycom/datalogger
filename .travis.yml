language: c++
compiler:
  - gcc
before_install:
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo add-apt-repository -y ppa:boost-latest/ppa
  - sudo apt-get update -qq
install:
  - sudo apt-get install -qq g++-4.8 libboost1.55-all-dev
  - export CXX="g++-4.8"
script:
  - make all
